# Система монет — Pay-per-use монетизация

## Что реализовано

### 1. База данных
✅ Таблица `users.coins` — баланс монет пользователей (начальный: 1000 💎)  
✅ Таблица `coin_transactions` — история всех операций

### 2. Backend API (`/backend/coins`)
**URL:** `https://functions.poehali.dev/932b9e21-131e-4c52-a414-a77b7dff1951`

**Эндпоинты:**
- `GET /?action=balance` — текущий баланс (требует авторизацию)
- `GET /?action=transactions` — история транзакций (требует авторизацию)
- `GET /?action=prices` — прайс-лист (публичный)
- `POST /?action=deposit` — пополнение баланса
- `POST /?action=withdraw` — списание монет

### 3. Прайс-лист действий

| Действие | Монеты | Описание |
|----------|--------|----------|
| Публикация курса | 500 💎 | За каждый новый курс |
| Публикация мастермайнда | 500 💎 | За каждый новый мастермайнд |
| Продвижение (1 день) | 200 💎 | Топ позиция на 1 день |
| Продвижение (3 дня) | 500 💎 | Топ позиция на 3 дня |
| Продвижение (7 дней) | 800 💎 | Топ позиция на 7 дней |
| Сообщение школе | 10 💎 | За каждое сообщение |
| Верификация профиля | 1000 💎 | Разовая оплата |

**Курс обмена:** 100 монет = ₽100

### 4. Интеграция в функции

#### ✅ `/backend/courses` — списание за публикацию
- При создании курса: `-500 💎`
- При создании мастермайнда: `-500 💎`
- Проверка баланса перед публикацией
- Автоматическая запись транзакции

#### 🔄 Планируется:
- `/backend/masseur-promotions` — уже работает с балансом, нужно переделать на монеты
- `/backend/messages` — 10 💎 за сообщение школе
- `/backend/submit-verification` — 1000 💎 за верификацию

### 5. UI компоненты

#### `CoinBalance` — виджет баланса
**Варианты:**
- `compact` — карточка для дашборда
- `full` — развёрнутая версия с кнопкой пополнения

**Использование:**
```tsx
import CoinBalance from '@/components/CoinBalance';

<CoinBalance variant="compact" />
```

#### `CoinsPage` — страница монет
**URL:** `/coins`

**Функции:**
- Просмотр баланса
- Прайс-лист действий
- История транзакций (100 последних)
- Кнопка пополнения

### 6. Интеграция в дашборд

✅ Добавлен в `SchoolDashboard` — виджет баланса  
✅ Роут `/coins` в `App.tsx`  
✅ Автоматическая навигация при клике на виджет

## Как работает списание

### Пример: Публикация курса

1. **Школа создаёт курс** через `/school/dashboard`
2. **Backend проверяет баланс:**
   ```python
   cur.execute(f"SELECT coins FROM {schema}.users WHERE id = {user_id}")
   if coins < 500:
       return {'error': 'Недостаточно монет'}
   ```
3. **Списываются монеты:**
   ```python
   cur.execute(f"UPDATE {schema}.users SET coins = coins - 500 WHERE id = {user_id}")
   ```
4. **Записывается транзакция:**
   ```python
   cur.execute(f"""
       INSERT INTO {schema}.coin_transactions 
       (user_id, amount, type, action, description)
       VALUES ({user_id}, 500, 'withdrawal', 'publish_course', 'Публикация курса: {title}')
   """)
   ```
5. **Курс публикуется** со статусом `pending`

## Пополнение баланса

### Текущая реализация
API готов к интеграции с платёжными системами:

```python
POST /coins?action=deposit
{
  "amount": 1000,
  "payment_id": "pay_123456",
  "description": "Пополнение через карту"
}
```

### Требуется интеграция:
- **ЮКасса / CloudPayments** — приём платежей
- **Webhook** для подтверждения оплаты
- **UI страница** `/coins/top-up` с формой оплаты

## База данных

### Таблица `users`
```sql
ALTER TABLE users ADD COLUMN coins INTEGER DEFAULT 0 NOT NULL;
```

### Таблица `coin_transactions`
```sql
CREATE TABLE coin_transactions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id),
    amount INTEGER NOT NULL,
    type VARCHAR(50) NOT NULL CHECK (type IN ('deposit', 'withdrawal')),
    action VARCHAR(100) NOT NULL,
    description TEXT,
    created_at TIMESTAMP DEFAULT NOW() NOT NULL
);
```

## Следующие шаги

### 1. Интеграция платёжной системы
- [ ] Подключить ЮКасса / CloudPayments
- [ ] Создать страницу `/coins/top-up`
- [ ] Добавить webhook для подтверждения оплаты

### 2. Списание за другие действия
- [ ] Сообщения школам: 10 💎
- [ ] Верификация профиля: 1000 💎
- [ ] Переделать продвижение массажистов на монеты

### 3. Уведомления
- [ ] Пуш при успешном пополнении
- [ ] Алерт при недостатке монет
- [ ] Email с чеком после оплаты

### 4. Админка
- [ ] Просмотр всех транзакций
- [ ] Ручное начисление/списание монет
- [ ] Статистика по выручке

## Тестирование

Все пользователи получили **1000 💎** для тестирования:
```sql
UPDATE users SET coins = 1000 WHERE coins = 0;
```

**Как проверить:**
1. Войти в личный кабинет школы
2. Увидеть виджет баланса с 1000 💎
3. Попробовать создать курс
4. Баланс должен стать 500 💎
5. Проверить историю на `/coins`

## API примеры

### Получить баланс
```bash
curl -H "Authorization: Bearer TOKEN" \
  https://functions.poehali.dev/932b9e21-131e-4c52-a414-a77b7dff1951?action=balance
```

### Получить прайс
```bash
curl https://functions.poehali.dev/932b9e21-131e-4c52-a414-a77b7dff1951?action=prices
```

### Списать монеты
```bash
curl -X POST \
  -H "Authorization: Bearer TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"action_type": "publish_course"}' \
  https://functions.poehali.dev/932b9e21-131e-4c52-a414-a77b7dff1951?action=withdraw
```
